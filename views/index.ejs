<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Cut</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<!-- style="display: flex; align-items: center; justify-content: center; flex-direction: column; min-height: 100vh;" -->

<body class="
    container-fluid
    text-center
    d-flex
    justify-content-center
    align-items-center
    flex-column
    bg-secondary-subtle" 
    style="min-height: 100vh;">
    <h1>Cortá la url</h1>
    <form action="/" method="post" class="col-4">
        <div class="row m-auto">
            <div class="input-group mb-3 ">
                <input type="text" name="longUrl" id="longUrl" class="form-control" placeholder="Ingresá la url"
                    aria-describedby="button-addon2">
                <button type="submit" value="Cortar" class="btn btn-primary col-4" id="button-addon2">Cortar</button>
            </div>
        </div>
    </form>
    <% if(errors.msg) {%>
        <div class="errors">
            <% errors.msg.forEach(error=> { %>
                <p class="text-danger text-center">
                    <%= error %>
                </p>
                <% }); %>
        </div>
        <% } %>
            <div class="url">
                <% if(url.link) {%>
                    <a href="<%= url.link %>" target="_blank">
                        <%= url.link %>
                    </a>
                    <% } %>
            </div>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
                integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
                crossorigin="anonymous"></script>
</body>

</html>